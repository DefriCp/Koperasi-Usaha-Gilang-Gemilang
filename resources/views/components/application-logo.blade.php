{{-- Logo lebar (dipakai di halaman login / auth) --}}
<div {{ $attributes->merge(['class' => 'flex items-center gap-2']) }}>
  <x-application-mark class="h-8 w-8"/>
  <span class="font-bold text-xl text-gray-900">Aplikasi</span>
</div>
